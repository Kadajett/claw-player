apiVersion: v1
kind: ConfigMap
metadata:
  name: relay-config
  namespace: openclaw
data:
  RELAY_MODE: "server"
  RELAY_PORT: "4000"
  REDIS_URL: "redis://:$(REDIS_PASSWORD)@redis.openclaw.svc.cluster.local:6379"
  LOG_LEVEL: "info"
  NODE_ENV: "production"
